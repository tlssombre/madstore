import Header from '@/components/Header';import BottomNav from '@/components/BottomNav';import CatalogueClient from '@/components/CatalogueClient';import {getStoreProducts} from '@/lib/catalog';
export const dynamic='force-dynamic';
export default async function Catalogue({searchParams}:{searchParams:Promise<{brand?:string;category?:string}>}){const products=await getStoreProducts();const sp=await searchParams;return <><Header/><main className="wrap catalog"><div className="catalogTitle"><div><span className="sectionKicker">CATALOGUE MADSTORE2</span><h1>Tous les produits</h1><p>{products.length} références publiées et synchronisées avec le stock.</p></div><a>Comparer ›</a></div><CatalogueClient products={products} initialBrand={sp.brand} initialCategory={sp.category}/></main><BottomNav/></>}
