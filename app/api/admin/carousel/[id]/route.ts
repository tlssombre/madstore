import {NextResponse} from 'next/server';
import {getApiStaff} from '@/lib/auth';
import {prisma} from '@/lib/prisma';
import {writeAudit} from '@/lib/audit';
const clean=(body:any)=>({internalName:String(body.internalName||'').trim(),eyebrow:String(body.eyebrow||'').trim()||null,title:String(body.title||'').trim(),subtitle:String(body.subtitle||'').trim()||null,ctaLabel:String(body.ctaLabel||'').trim()||null,ctaHref:String(body.ctaHref||'').trim()||null,imageUrl:String(body.imageUrl||'').trim()||null,mobileImageUrl:String(body.mobileImageUrl||'').trim()||null,background:/^#[0-9a-f]{6}$/i.test(String(body.background||''))?String(body.background):'#f2f3f5',textColor:/^#[0-9a-f]{6}$/i.test(String(body.textColor||''))?String(body.textColor):'#0b0d12',contentAlign:['CENTER','LEFT'].includes(String(body.contentAlign))?String(body.contentAlign):'CENTER',active:body.active!==false,sortOrder:Number.isFinite(Number(body.sortOrder))?Number(body.sortOrder):0,autoplayMs:Math.min(15000,Math.max(2800,Number(body.autoplayMs)||5200))});
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const auth=await getApiStaff('content.manage');if(!auth.ok)return NextResponse.json({error:'Accès refusé.'},{status:auth.status});const {id}=await params;const data=clean(await request.json().catch(()=>({})));if(!data.internalName||!data.title)return NextResponse.json({error:'Nom interne et titre requis.'},{status:400});const slide=await prisma.homeSlide.update({where:{id},data});await writeAudit(auth.staff.id,'HOME_SLIDE_UPDATED','home-slide',id,{name:slide.internalName,active:slide.active});return NextResponse.json({slide});}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const auth=await getApiStaff('content.manage');if(!auth.ok)return NextResponse.json({error:'Accès refusé.'},{status:auth.status});const {id}=await params;const slide=await prisma.homeSlide.findUnique({where:{id}});if(!slide)return NextResponse.json({error:'Slide introuvable.'},{status:404});await prisma.homeSlide.delete({where:{id}});await writeAudit(auth.staff.id,'HOME_SLIDE_DELETED','home-slide',id,{name:slide.internalName});return NextResponse.json({ok:true});}
