'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export default function ChangePassword(){const router=useRouter();const [currentPassword,setCurrent]=useState('');const [newPassword,setNext]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(newPassword!==confirm){setError('Les deux nouveaux mots de passe ne correspondent pas.');return}setLoading(true);setError('');const res=await fetch('/api/admin/auth/change-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({currentPassword,newPassword})});const data=await res.json();setLoading(false);if(!res.ok){setError(data.error||'Impossible de modifier le mot de passe.');return}router.replace('/admin');router.refresh()}return <div className="passwordPanel"><p className="eyebrow">SÉCURITÉ</p><h1>Choisir votre mot de passe</h1><p>Votre accès a été créé avec un mot de passe temporaire. Définissez maintenant un mot de passe personnel d’au moins 12 caractères.</p><form onSubmit={submit}><label>Mot de passe temporaire<input type="password" value={currentPassword} onChange={e=>setCurrent(e.target.value)} required/></label><label>Nouveau mot de passe<input type="password" minLength={12} value={newPassword} onChange={e=>setNext(e.target.value)} required/></label><label>Confirmer le nouveau mot de passe<input type="password" minLength={12} value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label>{error&&<div className="loginError">{error}</div>}<button className="loginSubmit" disabled={loading}>{loading?'Enregistrement…':'Enregistrer et continuer'}</button></form></div>}
